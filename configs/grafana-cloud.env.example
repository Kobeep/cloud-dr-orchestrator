# Grafana Cloud Configuration
# Sign up for free at https://grafana.com/auth/sign-up/create-user
# Free tier: 10k series, 50GB logs, 14 days retention

# Grafana Cloud Prometheus endpoint
# Find at: Grafana Cloud Portal -> Prometheus -> Remote Write
GRAFANA_CLOUD_PROMETHEUS_URL=https://prometheus-prod-XX-prod-XX-XX.grafana.net/api/prom/push

# Grafana Cloud username (usually numeric ID)
# Find at: Grafana Cloud Portal -> Prometheus -> Remote Write
GRAFANA_CLOUD_PROMETHEUS_USER=123456

# Grafana Cloud API Key
# Create at: Grafana Cloud Portal -> Security -> API Keys
# Permissions needed: MetricsPublisher
GRAFANA_CLOUD_API_KEY=glc_your_api_key_here

# Alternative: Local Prometheus (if not using Grafana Cloud)
# LOCAL_PROMETHEUS_URL=http://localhost:9090/api/v1/write
