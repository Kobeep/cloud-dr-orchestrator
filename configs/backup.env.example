# Cloud DR Orchestrator - Environment Configuration
# Used by systemd service for automated backups

# Backup configuration
BACKUP_NAME=automated-backup
OUTPUT_DIR=/opt/cloud-dr-orchestrator

# PostgreSQL connection
DB_NAME=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme

# Oracle Cloud Storage
OCI_BUCKET=your-bucket-name
OCI_COMPARTMENT=ocid1.compartment.oc1..xxxxxx
OCI_CONFIG=/root/.oci/config
OCI_PROFILE=DEFAULT

# Optional: Notification settings (future enhancement)
# WEBHOOK_URL=https://hooks.slack.com/services/xxx
# EMAIL_TO=admin@example.com
